Global state의 값이 필요한 자식컴포넌트들은 props를 통해 전달받을 것임

그러나 자식컴포넌트들의 자식컴포넌트들또한 GlobalState의 특정 값이 필요하다면
porps를 통해 전달 체이닝이 이뤄질 것임

그렇다면 개발 시 반복작업이 늘어나고, 코드 가독성과 개발편의성이 낮아질것임

그럴때 Redux를 사용하면, 필요한 컴포넌트들에서 필요한 값을 직접적으로 getState!

깔-끔